debug=${IS_DEBUG}
spring.jpa.database=POSTGRESQL
spring.datasource.url=jdbc:postgresql://${DB_DOMAIN_PORT}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.open-in-view=True
